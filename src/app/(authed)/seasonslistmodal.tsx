import { View, Text } from 'react-native';
import React from 'react';
import SeasonsContainer from '~/components/shows/details/seasons/SeasonsContainer';

const seasonslist = () => {
  return <SeasonsContainer />;
};

export default seasonslist;
