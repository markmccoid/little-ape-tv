import { View, Text } from 'react-native';
import React from 'react';
import ImagePickerContainer from '~/components/shows/details/imagePicker/ImagePickerContainer';

const detailimagemodal = () => {
  return <ImagePickerContainer />;
};

export default detailimagemodal;
