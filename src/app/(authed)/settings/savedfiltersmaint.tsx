import { View, Text } from 'react-native';
import React from 'react';
import SavedFiltersContainer from '~/components/settings/savedFilters/SavedFiltersContainer';

const Savedfiltersmaint = () => {
  return <SavedFiltersContainer />;
};

export default Savedfiltersmaint;
