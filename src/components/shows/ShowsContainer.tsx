import { View, Text } from 'react-native';
import React from 'react';

const ShowsContainer = () => {
  return (
    <View>
      <Text>ShowsContainer</Text>
    </View>
  );
};

export default ShowsContainer;
